export default (todo)=>{
    console.log(todo)
    // const db = firebase.firestore().collection('todos'); 
    // const id = firebase.auth().currentUser.uid;
    // try{
    //     await db.doc(id).set({
    //         todos: [todo]
    //     });
    // }catch(e){
    //     console.log(e);
    // }
}